{
  "name": "workout-1",
  "private": true,
  "version": "3.3.1",
  "description": "Ayanokoji System v3.3.1 - Progressive Web App with Bug Fixes",
  "scripts": {
    "start": "npx http-server -p 8080 -o",
    "dev": "npx live-server --port=8080",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "format": "prettier . --write",
    "format:check": "prettier . --check"
  },
  "devDependencies": {
    "eslint": "^9.25.1",
    "globals": "^15.15.0",
    "prettier": "^3.5.3",
    "vitest": "^2.1.9"
  }
}
