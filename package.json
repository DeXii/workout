{
  "name": "workout-1",
  "private": true,
  "version": "1.0.0",
  "description": "Ayanokoji workout tracker quality tooling",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "format": "prettier . --write",
    "format:check": "prettier . --check"
  },
  "devDependencies": {
    "eslint": "^9.25.1",
    "globals": "^15.15.0",
    "prettier": "^3.5.3",
    "vitest": "^2.1.9"
  }
}
